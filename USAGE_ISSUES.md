# 使用问题记录

日期：2026-06-09

## 连续出图实测：搞笑猫狗动漫一集

目标流程：

1. 通过浏览器打开连续出图。
2. 输入剧情，规划核心角色/对象/主题和分镜。
3. 先生成核心参考图。
4. 再引用核心参考图继续生成剧情图。

## 遇到的问题

1. 浏览器自动化批量输入不可用
   - 现象：`tab.cua.type` / `dom_cua.type` 依赖虚拟剪贴板，当前环境提示 virtual clipboard 未安装。
   - 影响：长剧情、中文文本无法通过批量粘贴式输入完成，只能逐键输入，效率低且容易影响数字输入。
   - 处理：新增“猫狗搞笑”剧情模板按钮，减少长文本输入；继续保留其他剧情模板。

2. 张数输入容易误操作
   - 现象：通过键盘自动化清空数字框时，曾把 `4` 输入成 `46`。
   - 影响：虽然程序会把张数钳制到最大 24，但用户可能不清楚实际会生成更多图。
   - 处理：在张数字段下新增 `4 / 6 / 8 / 12` 快捷按钮，点击即可替换张数。

3. 弹窗滚动后底部动作按钮不易操作
   - 现象：核心参考项和分镜内容较多时，底部“生成核心参考图 / 继续生成剧情图”按钮可能滚出当前可见区域。
   - 影响：自动化和用户都需要额外滚动才能继续下一步。
   - 处理：将连续出图弹窗的底部动作栏设为 sticky bottom，滚动时保持可操作。

4. 常规 Playwright 点击在部分按钮上偶发超时
   - 现象：`#storyboard-btn`、`#storyboard-start-btn` 常规 locator click 偶发超时，但 DOM 可见且页面无控制台错误。
   - 影响：浏览器自动化测试需要切换到 `dom_cua.click` 或稳定按钮 id。
   - 处理：操作层改用可见 DOM 节点点击；产品侧通过 sticky 动作栏降低按钮定位复杂度。

5. 参考图生成耗时长
   - 现象：核心参考图和引用多张参考图的剧情图单张耗时可超过 1 分钟。
   - 影响：长轮询容易超过工具调用时限。
   - 处理：改用短轮询分段监控，避免单次等待超过工具限制。

6. 引用多张参考图生成剧情图时可能触发上游临时错误
   - 现象：剧情图生成过程中出现 `An error occurred while processing your request`，页面自动进入失败重试。
   - 影响：用户可能误以为任务卡住；实际上重试后可恢复。
   - 处理：保留自动重试与过程日志；实测最后一张在自动重试后成功出图。后续可在 UI 上增加更明确的“正在重试，任务仍会继续”状态。

7. 最后一张剧情图引用全部参考图时多次失败
   - 现象：通过浏览器生成猫狗系列时，最后一张剧情图使用 4 张参考图，多次出现上游处理错误和 `fetch failed`。
   - 影响：连续出图整体完成度被最后一张拖住，用户体验像是尾帧不稳定。
   - 处理：代码改为每帧按分镜文本筛选相关核心参考图，最多传 3 张；角色优先，其次核心对象和匹配到的地点/主题，降低请求复杂度。

## 已验证的关键状态

- 规划完成后：核心参考图按钮启用，剧情图按钮禁用。
- 参考图全部完成后：剧情图按钮启用。
- 点击继续生成剧情图后：弹窗关闭，聊天区出现生成任务。
- 后端生成代理会把参考图作为 `input_image` 传给上游。
- 引用参考图的剧情图出现临时上游错误时，自动重试后可继续完成。
- 剧情帧参考图策略已从“全部引用”改为“按相关性最多 3 张”。
